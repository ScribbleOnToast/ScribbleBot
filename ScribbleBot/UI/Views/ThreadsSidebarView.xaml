<UserControl x:Class="ScribbleBot.UI.Views.ThreadsSidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="+ New Thread" Command="{Binding CreateNewThreadCommand}" Padding="10,8" Margin="0,0,0,8" Background="#007ACC" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"/>

        <Button Grid.Row="1" Content="🔍 Search Chats" Command="{Binding SearchChatsCommand}" Padding="10,8" Margin="0,0,0,10" Background="#333337" Foreground="#F1F1F1" BorderThickness="0" Cursor="Hand"/>

        <Button Grid.Row="2" Command="{Binding SettingsCommand}" Padding="10,8" Margin="0,0,0,10" Background="#333337" Foreground="#F1F1F1" BorderThickness="0" Cursor="Hand">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Settings" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

        <Rectangle Grid.Row="3" Height="1" Fill="#3F3F46" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding State.Threads}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid x:Name="ThreadRow" Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0"
                                    Command="{Binding DataContext.SwitchThreadCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Padding="10,8" Background="#2D2D30" Foreground="#F1F1F1" HorizontalContentAlignment="Left" BorderThickness="0" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                                <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis"/>
                            </Button>

                            <Button x:Name="DeleteButton"
                                    Grid.Column="1"
                                    Command="{Binding DataContext.DeleteThreadCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Content="✕" Foreground="#F44336" Background="#2D2D30" BorderThickness="0" Padding="10,0" FontWeight="Bold" Cursor="Hand" Visibility="Collapsed">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                        <DataTemplate.Triggers>
                            <Trigger SourceName="ThreadRow" Property="IsMouseOver" Value="True">
                                <Setter TargetName="DeleteButton" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>